# NPM configuration for HypeShelf

# Security: Always run audit
audit=true

# Security: Fail on high/critical vulnerabilities during install
audit-level=high

# Consistency: Use exact versions to prevent supply chain drift
save-exact=true

# Performance: Use lockfile
package-lock=true

# Security: Warn about deprecated packages
fund=false

# Engine strictness
engine-strict=true
