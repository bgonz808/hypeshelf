# HypeShelf Environment Variables
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local or any file with real secrets

# =============================================================================
# CLERK AUTHENTICATION
# Get these from: https://dashboard.clerk.com → API Keys
# =============================================================================

# Public key - safe for client-side, embedded in browser bundle
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# Secret key - server-side only, NEVER expose to client
CLERK_SECRET_KEY=sk_test_REPLACE_ME

# Issuer URL - used by Convex to verify Clerk JWTs
# Format: https://[your-clerk-subdomain].clerk.accounts.dev
CLERK_ISSUER_URL=https://your-subdomain.clerk.accounts.dev

# Clerk redirect URLs (optional, defaults work for most cases)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# =============================================================================
# CONVEX DATABASE
# Get these from: https://dashboard.convex.dev → Settings
# =============================================================================

# --- DEVELOPMENT ENVIRONMENT ---
# Used for: local dev (npm run dev), Vercel preview deployments

# Deployment URL - public, identifies your Convex backend
NEXT_PUBLIC_CONVEX_URL=https://your-dev-deployment.convex.cloud

# Deploy key - secret, used for CI/CD deployments only
# Not needed for local dev (Convex CLI handles auth via browser)
CONVEX_DEPLOY_KEY=prod:REPLACE_ME_DEV_KEY

# --- PRODUCTION ENVIRONMENT ---
# These are configured in Vercel for production deployments
# Kept here for documentation purposes
#
# NEXT_PUBLIC_CONVEX_URL=https://your-prod-deployment.convex.cloud
# CONVEX_DEPLOY_KEY=prod:REPLACE_ME_PROD_KEY

# =============================================================================
# APPLICATION
# =============================================================================

# Base URL for the application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin bootstrap - email that gets admin role on first sign-in
# Remove after initial admin is set up
INITIAL_ADMIN_EMAIL=your-email@example.com

# =============================================================================
# OPTIONAL: DEVELOPMENT OVERRIDES
# =============================================================================

# Set to 'development' or 'production'
NODE_ENV=development

# Next.js port (default: 3000)
PORT=3000
